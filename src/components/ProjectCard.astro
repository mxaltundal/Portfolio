---
const { title, description, image, category, github, link } = Astro.props;
---

<div class="rounded-xl overflow-hidden shadow-lg bg-white/70 backdrop-blur-md hover:scale-105 transform transition p-4">
  <img src={image} alt={title} class="w-full h-48 object-cover rounded-md mb-4" />
  <h2 class="text-lg font-semibold text-gray-800">{title}</h2>
  <p class="text-sm text-gray-600 mb-3">{description}</p>
  <p class="text-xs text-candy font-medium mb-3">#{category}</p>
  <div class="flex space-x-4 text-sm">
    {github && <a href={github} class="text-blue-600 hover:underline" target="_blank">GitHub</a>}
    {link && <a href={link} class="text-pink-600 hover:underline" target="_blank">Live</a>}
  </div>
</div>